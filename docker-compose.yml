services:
  homelab-mcp:
    build: .
    container_name: homelab-mcp
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - MCP_TRANSPORT=sse
    env_file:
      - .env
