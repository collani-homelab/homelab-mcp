# homelab-mcp Task Tracker

- `[ ]` uncompleted tasks
- `[/]` in progress tasks (custom notation)
- `[x]` completed tasks

## Current Sprint

- [x] Documentation Refactor: Update README.md to remove legacy `Design/` folder links.
- [x] Context Modernization: Update `README.md` to point to local root `ARCHITECTURE.md` and `ROADMAP.md`. Update their contents and `GLOBAL_CLAUDE.md` to reflect the new `AGENTS.md` pattern and shared runner topology.

## Next Session / Sprint

- [ ] CI/CD Prep: Verify/Create `.github/workflows/deploy.yml` for building and pushing the Docker image.
- [ ] Platform Prep: Validate `platform.json` has all correct environment variables defined.
- [ ] Cross-Repo: Note deployment requirements for `homelab-platform` (sre-machine.json update and shared runner verification).
